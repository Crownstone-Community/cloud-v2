
export class CommandEventHandler {
  // sendStoneMultiSwitch(sphere: SphereData, stones: DataObject<Stone>[], switchStateMap: SwitchStateMap) {
  //   let packet = SSEPacketGenerator.generateMultiSwitchCrownstoneEvent(sphere, stones, switchStateMap);
  //   SSEManager.emit(packet);
  //   return packet;
  // }
}

